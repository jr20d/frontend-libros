(function(window) {
  window.env = window.env || {};

  // Environment variables
  window["env"]["url_base_libros_service"] = "${URL_BASE_LIBROS_SERVICE}";
  window["env"]["debug"] = "${DEBUG}";
})(this);
